# Optional: real email via Nodemailer (omit both to log-only mode)
# SMTP_URL=smtps://user:pass@smtp.example.com:465
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# MAIL_FROM="Demo <you@example.com>"
# PORT=3000
